stages:
- stage: Deploy
  jobs:
  - deployment: DeployJob
    displayName: Deploy to Environment
    environment: 'MyEnvironmentName' # Replace with your environment name
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "Deploying to environment"
